-- determine the birth year of Emma Stone
-- output a table with a single column and a single row (plus optional header) containing Emma Stone’s birth year
-- 1 column and 1 row

SELECT birth FROM people WHERE name = "Emma Stone";